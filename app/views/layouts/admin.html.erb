<!DOCTYPE html>
<html>
    <head>
        <title>HACKSCHOOL </title>
        <%= csrf_meta_tags %>

        <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
        <%= stylesheet_link_tag    'simplemde.min' %>
        <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
        <%= javascript_include_tag 'simplemde.min' %>
    </head>

    <body>

            <%= render "common/navbar" %>
            <div class="container">
            <%= render "common/flashes" %>
            <div class="row">
              <div class="col-md-2">
                <ul class="nav nav-pills nav-stacked" style="max-width: 300px; padding-top:20px;">
      <li class="active"><%= link_to("Courses", admin_courses_path, class: "btn btn-primary" ) %> </li>

      <li class="active"><%= link_to("Users", admin_users_path, class: "btn btn-primary" ) %> </li>
                 </ul>
              </div>

              <div class="col-md-10">
                <%= yield %>
              </div>
            </div>
        </div>

    </body>
    <div class="container">
      <%= render "common/footer" %>
    </div>
</html>
