<%= simple_form_for [:account, @course, @question, @answer] do |f| %>
  <div class="form-group">
    <%= f.input :content, input_html: {class: "form-control"} %>
  </div>
  <%= f.submit "Submit", class: "btn btn-default", data: {disable_with: "Sumbitting..."} %>
<% end %>
