<nav class="navbar navbar-fixed-top navbar-inverse" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to("HackSchool",courses_path, id: "logo") %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right ">
        <li><a href="#" data-toggle="modal" data-target="#year", class="btn btn-xs btn-danger", style="color:white">年费会员</a></li>

        <% if !current_user %>
          <li style="font-size:18px;"><%= link_to("log in", new_user_session_path) %></li>

          <% else %>
          <!-- <li><%= link_to("成为年费会员", "#", class: "btn btn-xs btn-default") %></li> -->
          <li class="dropdown">
            <a href="#" class="dropdown-toggle  " data-toggle="dropdown">

                <%= render_user_avatar(current_user, :tiny) %>
    

                    Hi!, <%= current_user.email %>
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">

              <% if current_user.admin? %>
                  <li><%= link_to("Admin Panel", admin_courses_path) %> </li>
              <% end %>

              <li> <%= link_to("User Panel", account_courses_path) %> </li>
              <li> <%= link_to("Settings", account_settings_path) %> </li>
              <li> <%= link_to("Discussion", questions_path) %> </li>

              <li class="divider"></li>
              <li><%= link_to("Sign out", destroy_user_session_path, method: :delete) %></li>
            </ul>
          </li>
          <% end %>
      </ul>

    </div>
  </div>
</nav>
<!-- madal -->
<div class="modal fade" id="year">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title">年费会员</h4>
      </div>
      <div class="modal-body">
        <p>成为年费会员可以享受一年内全网课程全部免费报&hellip;</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary">成为年费会员</button>
      </div>
    </div><!-- /.modal-content -->
  </div><!-- /.modal-dialog -->
</div><!-- /.modal -->
